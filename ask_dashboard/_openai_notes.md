Rule routing handles the new contribution questions before the optional AI fallback is considered. The existing AI intent schema automatically includes `alliance_positive_contribution_leader` through the exported supported-intent set. A future cleanup can add a dedicated natural-language definition for this intent directly in `openai_intent.py` when the compatibility layer is folded back into the main module.
